# Simple Video Player (svplay)

## Install

```bash
sudo apt update
sudo apt install libsdl2-dev libavcodec-dev libavformat-dev libavutil-dev libswscale-dev
```

## References

- [ffmpeg](https://ffmpeg.org/)
